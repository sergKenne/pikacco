import React from 'react';

const ErrorMessage = () => {
    return (
        <div style={{ fontSize: '18px', paddingTop: '20px' }}>
            Some thing run wrong...
        </div>
    );
};

export default ErrorMessage;
